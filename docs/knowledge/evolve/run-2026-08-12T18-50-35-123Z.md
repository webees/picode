# Evolve run-2026-08-12T18-50-35-123Z

- goal: prime-agent 机制学习 → picode 优化（问题台账全量处置）
- kind: self_evolve
- layers: docs, knowledge, tests, code
- risk: medium
- baseline: main

## Intent

研究 https://github.com/PrimeIntellect-ai/prime-agent 机制，对照 picode 现状产出并实施优化；
docs/problems/2026-08-13-intake.md 问题台账（A 运行错误/B 开放项/C 审计债务/D 交接问题）逐项处置。

## 产出

- C1（docs/problems/2026-08-13-decisions.md）：run-lead 决策清单——每项问题 修/缓/拒 + 根因与理由
- C1（docs/knowledge/prime-agent-study.md）：prime-agent 机制映射（sys-arch 评估：心跳重附/checkpoint/技能包借鉴；daemon/rlm/自主模式不采纳）
- C2（5 项修复，经 run-lead 决策 + T06 门闩三次拦截后合并）：
  - ERR-05 根因修复：buildPiEnv 补注入 PICODE_RUN_ALLOWLIST（空 allowlist 全拒）
  - ERR-03：PICODE_CWD worktree-aware（prepare 后指向 worktree）+ ready message 写入引导
  - O005 补全：verify_commands 默认 `npm run build && npm test`（防测旧 dist）
  - 审计第 6 项：merge awake 判断改 SessionStore.get（弃正则扫 YAML）
  - ERR-01 watchdog：self-drive probeServeHealth（serve 失联标记 awake 会话 error，不自动重 spawn）
  - 附带：mcp-server schema.ts zod 4 兼容（ZodRawShape Readonly）、glob 零层匹配边界修正

## Verification

- npm run build 零错误；npm test 230 断言全绿（+2 新测试）
- T06 diff⊆write_paths 门闩全程生效（3 次拦截均为真实偏差：write_paths 未覆盖 watchdog/schema.ts、**/*.test.ts 零层不匹配）

## 监督过程记录（编排者视角）

- serve 会话非受管（无 PICODE env/工具/cwd 偏差）→ 重启 serve（cwd=克隆仓）+ 权限收敛（bash allow 只读原则、edit ask、webfetch deny）+ auth.json 取 key
- noReply 消息 serve 不处理（tokens=0）→ 同步投喂 + 消息历史轮询
- 模型卡 serve permission ask 无人应答 → 配置化解决
- run-lead/scout/sys-arch 真实会话产出方案 → 监督者经 MCP 实施

## Remaining risks

- serve stream 挂起（ERR-01 上游）与权限 ask 挂起：watchdog 已做可观测，根治依赖上游
- C3 测试夹具抽取（28 文件重复）未实施（run 内待办，下轮）
- 会话中间态 checkpoint（prime-agent /refine 对应物）未做（列为后续轮）

## 追加：五路并行轮（C3/C5/C6/C7/C8）

- C3 refactor(test)：21 文件测试夹具单源化（test-utils.gitInit），-60+ 行重复
- C5 docs(standards)：提交信息规范 commit.md（type(scope): 摘要 + body 根因 + Reviewed-by footer）+ git_commit 工具与提示词注入
- C6 build(scripts)：test 前清 tsbuildinfo（防 D062 陈旧 dist 现场复现）
- C7 docs(ops)：运维规程（serve 重启/会话重建/文件真相）+ O006 check_signoff 格式草案
- C8 refactor(core)：HUMAN_ONLY_ROLES 收敛（死符号转生产引用，清审计债务 C.2 一项）

五路并行：写集互斥验证通过（run-lead 批准），串行 merge 列车（D036），E4 gate 全程生效。

## 追加：深度研究轮（P1-P6 + intake + skills）

- C1 feat(orchestrator)：serve 自动恢复（有界重试阶梯 + 重投喂 ready）+ 会话转录归档（transcript-store，P1+P4）——engineer 真实会话实施
- C2 feat(orchestrator)：evolve refine 子命令（evidence→lesson 自动提炼 + 审批门，P3）——engineer 实施
- C3 feat(orchestrator)：窗口语义摘要层（summary/summary_due，P2）——engineer 实施
- intake feat：sponsor 信息入口 add/triage/close + 看板可见 + MCP 工具（sponsor 核心诉求「随时投喂→分诊分发」）
- docs-opt docs：技能体系索引（M1/M4/M5，mattpocock 研究落地，docs cell 实施）
- C4 skills（P5/M1-M5 代码层）：进行中

监督实录：serve 多次重启（external_directory 权限卡死→配置放行）；fetch failed 与消息送达不一致（以消息历史为准）；真实 LLM 会话独立实施（C1/C2/C3/intake/docs）。

## 追加：C4 skills 合并 — run 全部完成

- C4 feat(orchestrator)：skills 技能包激活（SkillsConfig 去 D055 死键 + skills.ts 索引加载 + ready message 注入 + 校验测试）——engineer 真实会话实施
- run 13 个 chunk 全部合并；E6 本纪要即最终归档

监督实录：C4 会话 25 分钟独立完成（config/加载器/注入/测试全链路），监督者仅验证（246 断言绿）+ 门闩收尾。

## 追加：ponytail 应用轮（PT1/PT3）

- PT1 feat(skills)：ponytail 种子技能（ladder 七步/根因优先/ponytail: 标记）落 skills/engineering/ + 索引登记 + 人读页（a03c9b5）
- PT3 feat(skills)：审查 Checklist 增 ponytail 三维度（死代码/重复/过度设计）+ ladder 对照（d671fe0）
- 缓（台账）：PT2 提示词纪律注入（技能文本稳定后由文档小组做）、PT4 mcp/pi-extension 对照、PT5 benchmarks 度量
- 来源：dietrichgebert/ponytail（MIT；实测 LOC -54%/cost -20%/time -27%/安全 100%）

## 追加：agency-agents 融合轮（A1/A2/A3/A4）

- C1 docs：人设模板定稿（frontmatter 4 必填+2 可选 + Identity/Mission/Rules/Metrics 四节）+ 17 §6 引用 + 重写清单（e3362f5）
- C2 feat(core)：persona-lint 校验（agents 目录↔注册表一致 + frontmatter 完整性含 success_metrics，npm run check）（3e9ddc9）
- C3 docs(agents)：种子人设重写（engineer/sdet/squad-lead/run-lead 四节模板）（9b6902f）
- 缓：A5 多工具导出、A6 vibe（台账）
- 来源：msitarzewski/agency-agents（MIT，人格深度 + division 单源 + CI 校验思路）

## 追加：prime-agent 源码深挖二轮（Q1/Q3 融合）

- C1 feat(orchestrator)：会话预算边界（maxTurns/gate 停靠，防 LLM 失控循环；f6820e9）
- C2 feat(core)：E6 写入冲突检测（baseline 比对 + 冲突拒绝；7d8bdf5）
- 缓（台账）：Q2 auto-refine gate（依赖 LLM 评审，等 refine 场景成熟）、Q4 摘要预算（低价值）
