<!-- 文档小组产物。authored_by: docs-lead@run-2026-08-15T04-00-00-DASHBOARD · drafted_by: tech-writer · checked_by: docs-qa · date: 2026-08-15 -->
<!-- 状态：终稿（W3 chunk-docs 交付 + run-lead 合并门后收尾）。决策编号 D113-D118 落地（--land 完成）；decision-lint 0 error；docs:lean OK；全部 5 chunks 已 merge 并 push。 -->

# Evolve run-2026-08-15T04-00-00-DASHBOARD（E19 纪要 · 终稿）

- goal: 管理界面完善——聊天室化（bus/rooms 消息流 + 可发送）+ 流程可视化 + 流程项落地
- kind: self_evolve · scale: M（3 波次：W1 双组并行 → W2 双组并行 → W3 docs 收尾；D036 串行 merge）
- baseline: main = 347f206（文档精简完成后，decision-lint 120 OK）→ 终态 **0929496**（全部 5 chunks 合并）
- status: **completed**（W1a a55e05f / W1b ab78cab + 78d4aa4 / W2a 19e006c / W2b 0929496 / W3 12b2378 全部 merged 且门禁通过；push 同步）
- 决策编号: **D113-D118**（本 run 领取落地，--reserve（count 6，run-lead 预留 113-118）→ 表行+详条 → --land 闭环）

## 分块与合并序列（D036 串行 merge · 5 chunks）

W1 chunk-dashboard-server（观澜队：凭栏/听涛/漱石，D113/D114）∥ chunk-process-items（铨衡队：品藻/铨叙/月旦，D117）
并行 → W2 chunk-chat-ui（雁书队：青鸟/尺素/鱼雁，D116）∥ chunk-flow-ui（砥柱队：司关/钤印/稽核，D115）并行 → W3
chunk-docs（D118，本文档）。波序：W1（2 组并行）→ W2（2 组并行，峰值 ≤3 ✓）→ W3。依赖图：chat-ui/flow-ui
均 depends_on dashboard-server；docs depends_on 全部四 chunk。写集两两互斥（shared_files 三处 owner 声明：
picode.api.ts / index.vue / labels.ts owner=chat-ui，operations.md owner=docs）。

## 实施纪要（各决策详情见 DECISIONS 详条与 catalog §25）

### 聊天室读面 + 写代理（W1a · 观澜 · D113/D114）

- D113 读面扩展：`GET /bus`（房间列表，与 statusSnapshot.rooms 同源口径）/ `GET /bus/:room`（消息流，
  `?limit=` 默认 50、损坏行容错、SAFE_ROOM_RE 防路径逃逸）/ `GET /bus/:room/members`（参与者）+ `GET
  /approvals`（ApprovalStore.list 升序）+ `GET /change-orders`（readChangeOrders ts 升序，供 flow-ui）。
- D114 唯一写端点：`POST /bus/:room` sponsor 身份 post type=chat（D018/D035 不变）；校验链走 RoomStore.post，
  ACL fail-closed（ROOM_POST_DENIED 403 / BUS_TYPE_DENIED 400 / BAD_ROOM 400 / BAD_BODY 400 / ACL_CORRUPT 500）；
  其余非 GET 仍 405；CORS 补 POST + OPTIONS 204。
- 详见 DECISIONS D113/D114。

### 流程项落地（W1b · 铨衡 · D117）

- queryTalentPool 只读消费入口（grade/skills/seat 筛选、S/A 级优先）+ `picode staffing pool` 子命令
  （--grade/--seat/--skill，只读零写、非法 grade 结构化拒绝）；createStaffingRequest 显式预填
  reuse_persona_ids（不新增配置键）。
- scripts/doc-lean-check.mjs（零依赖只读结构检查）+ package.json `docs:lean`（可作 merge gate 输入）。
- 详见 DECISIONS D117。

### 聊天室前端（W2a · 雁书 · D116）

- 「聊天室」tab（index.vue）+ rooms-view 房间卡片点击进入 + chat-room-view 消息流（类型徽章中文 labels、
  body/refs/meta 详情可展开）+ 参与者面板 + chat-send-box 发送框（POST /bus/:room，成功清空+刷新，
  未授权结构化中文提示，非 sponsor 可发房禁用）+ chat.data.ts 纯函数 + chat.test.ts fixture。
- 轮询 3s；骨架屏/ErrorState（D071 语义 token）；审批联动语义一致（本 chunk 不落盘审批请求，D114 fail-closed）。
- 详见 DECISIONS D116。

### 流程可视化 + 9 视图增强（W2b · 砥柱 · D115）

- flow.api.ts（useApprovals/useChangeOrders 3s 轮询，enabled+refetchIntervalInBackground）+ 门禁 tab 审批流区
  （pending/approved/rejected/used 徽章 + asked/decided 成对 + 沙箱 mode 中文）与变更单区
  （proposed→applied→closed 状态 + 时间线事件）+ 任务门禁流水卡（每任务 stage 徽章：双门闩→进行中→
  验证→交接→已合并/失败/已解散，纯派生 flow.data.ts deriveGateStages）+ 9 视图增强
  （看板双门闩徽标 / 合并门拓扑等待原因列（D045 merged|failed 满足语义，对齐 merge.ts depSatisfied）/
  概览审批待办+变更单活跃告警卡（统计区 4→6 卡）/ 进度·人员双门闩列 / 分块依赖状态徽章 /
  会话预算用尽徽标）。实测：vitest 6 文件 59/59 全绿；vue-tsc -b + vite build 通过；
  E5 审查两轮（首轮 needs-work 2 P0/3 P1 → 修复 → 复审 pass），merge 0929496。

### 9 视图增强清单（总览）

|视图|增强内容|chunk|
|------|------|------|
|概览|审批待办数/变更单活跃数告警卡片|flow-ui（D115）|
|进度|双门闩状态列|flow-ui（D115）|
|房间|房间卡片可点击进入聊天室（入口增强）|chat-ui（D116）|
|人员|轻量增强（语义色/域组件一致）|flow-ui（D115）|
|分块|轻量增强|flow-ui（D115）|
|看板|双门闩状态徽标（brief/staffing latch）|flow-ui（D115）|
|会话+tokens|轻量增强|flow-ui（D115）|
|合并|合并门展示增强（拓扑依赖/等待原因）|flow-ui（D115）|
|门禁|审批流区 + 变更单区 + 门禁状态机展示|flow-ui（D115）|
|聊天室（新 tab）|消息流/参与者/发送框|chat-ui（D116）|

### 聊天室化清单（D113/D114/D116）

- 读面：房间列表 / 消息流（?limit= 默认 50）/ 参与者 3 端点（D113）；写面：POST /bus/:room sponsor chat
  写代理（D114，唯一写端点）；前端：「聊天室」tab + 消息流视图 + 参与者面板 + 发送框 + 房间入口（D116）。
- 数据源 = bus/*.jsonl + rooms/<room>/members（文件真相 D002）；发送走 bus API + ACL fail-closed
  （product_acceptance「未授权拒绝」满足）。

### 流程项落地清单（D117）

- 评分画像消费（queryTalentPool + staffing pool 子命令）→ 招聘 reuse_persona_ids 显式引用；文档精简维护
  （doc-lean-check + docs:lean）。

## 验证（W3 草稿态）

- W1a：dashboard-server 全量 31+2skip + curl 冒烟（200/403/400/405）+ 回归断言（limit/损坏行/逃逸/字段原样）
  （review task-chunk-dashboard-server，结论 pass）
- W1b：全量 **379/379** + doc-lean-check exit 0 + pool CLI 实测（records=3 → 筛选 1/1/3/0）+ hr-talent.test +
  cli.test pool 用例（review 结论 pass）
- W2a：`pnpm -C packages/dashboard test`（vitest 全绿）+ `pnpm -C packages/dashboard build` 通过
  （merge 19e006c 门禁 pass）
- W3：decision-lint **0 error**（docs/** 全量扫描）；`node scripts/doc-lean-check.mjs` OK；diff 门禁 =
  5 写集文件 ⊆ write_paths（纯 docs 层 + watermark，零代码零配置）；决策编号闭环 `--reserve`（113-118，
  count 6）→ 落档 → `--land`（status=landed）；`git push origin main`（rev-list origin/main..main = 0）
- W2b：`vitest run` 6 文件 **59/59 全绿**（flow.test.ts 19/19）+ `vue-tsc -b` exit 0 + `vite build` 通过
  （E5 审查 task-chunk-flow-ui-e5 首轮 needs-work → 修复（TS2345/门禁流水接线/colspan/D045 语义）→
  复审 e5-r2 pass；merge 0929496 门禁 pass）
- 主仓全量回归：`npm test`（见 goal 完成记录，全绿）+ 三 lint 0 error

## 剩余风险（W3 草稿态）

- **serve 侧会话累积 GC**（D109 遗留，跨轮候选延续）：归档/清理列后续轮候选，非本轮验收
- **流程简化 A 级试点**（D112/E18 后续候选延续）：双人组/程序化人设/交接包精简排下一轮流程优化 run，
  本轮不实施
- **feedback/README 索引**（doc-lean-check warn 项）：DOC-LIFECYCLE §2 索引表转正项，W3 chunk-docs 已
  覆盖其他项，README 索引留待后续 docs 轮次

## 后续候选

1. **流程简化 A 级试点**（D112/E18）：排下一轮流程优化 run；验收口径改可测基线
3. **serve 会话累积归档/GC**（D109 遗留）：独立立项
4. **审批请求落盘（面板侧）**（D114/D116 观察）：发送框与审批流语义一致但面板不落盘审批请求，
   如需面板侧发起审批（写面放大）须重新立项走 D089 领号
