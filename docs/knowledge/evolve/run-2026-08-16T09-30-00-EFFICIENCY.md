<!-- 文档小组产物。authored_by: docs-lead@run-2026-08-16T09-30-00-EFFICIENCY · drafted_by: tech-writer · checked_by: docs-qa · date: 2026-08-16 -->
<!-- 状态：终稿（W3 chunk-docs 交付）。决策编号 D119-D124 落地（--land 完成，watermark next=125）；decision-lint 0 error；docs:lean OK；三 chunk 已 merge 并 push。 -->

# Evolve run-2026-08-16T09-30-00-EFFICIENCY（E20 纪要 · 终稿）

- goal: 会话失能机制修复（工具自检/工作房门闩/零产出看门狗/复活重投喂）+ 运行效率工具链（worktree/test 隔离/巡检脚本/测试编排）+ 流程快赢规范
- kind: self_evolve · scale: M（3 波次：W1 双组并行 → W2 watchdog → W3 docs 收尾；D036 串行 merge）
- baseline: main = 9b782c8（R16 DASHBOARD 收尾后）→ 终态 **e41cc3e**（三 chunk 全部合并）
- status: **completed**（3cfaaba toolchain / 9f93100 env-gate / e41cc3e watchdog 全部 merged 且门禁通过；修复波 22ed0a7/9907341 + 状态提交 a5de16f/313023a 已落地；评分回路由 people 评分闭合提交 **c481d26**（name-ledger/personas×9/teams×3/talent 聚合）收尾；push 同步）
- 决策编号: **D119-D124**（本 run 领取落地，`--reserve --run run-2026-08-16T09-30-00-EFFICIENCY --count 6` → 表行+详条 → `--land` 闭环，next_number 119→125）
- 三队: 金汤（env-gate：虎符/鱼钥/关防）、陶钧（toolchain：执规/斫轮/持矩）、金柝（watchdog：更筹/戍鼓/宵柝）

## 根因（双分析共识 · sys-arch + proc-audit）

1. **子代理工具 ENOENT**：会话默认 cwd 失效 + node/npm 不在 PATH → bash spawn 静默卡死无结构化上报（R16 根因 #1；仓外 DSH runtime-commands 软链 node/npm/npx/rg 已修，唯一治本 = 所有 bash 显式带 workdir）
2. **工作房从未真实创建**：spawn 落在错误目录（R16 根因 #2；M4 门闩 + M2 探测双落点拦截）
3. **无自动 at_risk/接管机制**：2 轮零产出红灯、4 轮接管候选通知全靠人工（M1 看门狗机制化）
4. **接管绕过审查门轨迹**：代实现/代修须走变更单式留痕 + review 版本化门禁（修复波）
5. **评分回路断裂**：scores/talent 聚合未闭环（收尾 checklist 固化，process-quickwin §4）

## 分块与合并序列（D036 串行 merge · 3 chunks）

W1 chunk-env-gate（金汤队：虎符/鱼钥/关防，D120）∥ chunk-toolchain（陶钧队：执规/斫轮/持矩，D123）
并行 → W2 chunk-watchdog（金柝队：更筹/戍鼓/宵柝，D119/D121/D122）→ W3 chunk-docs（本文档 + D119-D124
落档 + catalog §26 + operations + process-quickwin + name-ledger）。依赖图：watchdog depends_on env-gate
（TOOL_ENV_BROKEN + pi-adapter 探测契约）→ W2 等 W1 合并；docs depends_on 全部实现块。

合并序列（串行，每次一合并，approvals/merge.yaml 批准）：

| merge | 内容 | 规模 | 审查 |
|---|---|---|---|
| **3cfaaba** | toolchain 工具链 5 脚本 + 测试编排 | 6 文件 +805/-1 | E5 一轮 pass |
| **9f93100** | env-gate 工具探测 + worktree 门闩 | 7 文件 +479/-20 | E5 两轮 pass（22ed0a7 修复后）|
| **e41cc3e** | watchdog 零产出看门狗 + 自治循环修复 | 11 文件 +636/-25 | E5 四轮 pass（r1 needs-work → r4 pass）|

状态/修复波提交（W1 双合并后陆续落地）：22ed0a7（env-gate E5 P1 canonical 裁决 + P2-1）、9907341
（toolchain 修复波：签收门/review 门禁/假绿修复/词边界）、a5de16f（kb-triage 首跑）、313023a
（r17-fix-wave-record.md 修复波留痕）。

## 机制修复（M1-M5）

- **M1 零产出看门狗**（W2 watchdog · D119）：按 agent_id 追踪产出信号（git 提交/工作房 mtime/转录
  incoming），2 轮→at_risk+steer（I1 档），4 轮→bus 通知 run-lead+takeover_candidate；`TOOL_ENV_BROKEN:`/
  `WORKTREE_MISSING:` 错误前缀→**立即 at_risk**；零 LLM 纯规则；幂等（投递成功才置位）；guardianTick 接线。
  E5 四轮：r1 1P0+5P1（bus ACL 恒拒 P0、平台席误计、转录信号缺失、notify 不重试、锁外截断）→ r4 pass
  （P0-1 sess-mgr 投递身份、P1-A~P1-D 修复 + continuation 增量 + lib.mjs 注释清理）。
- **M2 工具环境自检**（W1 env-gate · D120）：probeCoreTools 纯函数（fs X_OK 不 spawn），wakeAgent spawn 前
  探测 bash/node/npm/git，缺失→TOOL_ENV_BROKEN 结构化拒绝（PicodeError.details.missing）。
- **M3 复活重投喂**（W2 watchdog · D119 接线）：guardianTick 集成 watchdog 复用既有节奏；probeServeHealth
  既有恢复语义不破坏（复活重投喂方向由 E5 记录为未扩展项，P2 延续）。
- **M4 工作房门闩**（W1 env-gate · D120）：prepareTask 存在→assertWorktreeExists 校验（伪目录拒绝）/缺失→
  自动创建（方案 D）/失败→WORKTREE_MISSING 中文提示；wakeAgent spawn 前缺失拒绝（不产生进程）；
  **canonical 布局裁决 = `<repo>/.picode/worktrees/squad-<taskId>`（顶层，不嵌套 runId，E5 P1 22ed0a7）**。
- **M5 开工自检模板**（W3 docs）：spawn 前工作房核验 + 三工具探活 + 基线确认 + 环境留痕（process-quickwin §6）。

## 修复波（修复波留痕见 r17-fix-wave-record.md）

- 机制层：merge-gate 签收门（acceptance accepted_by 非空，R4）+ review 版本化门禁（[5]）+ test-iso
  dashboard vitest 缺失→fail（P1-6 假绿修复）+ tour-check `grep -qw BLOCKED` 词边界（P2-14⑥）。
- 自治循环并入（W2 watchdog）：P1-2 队列消费至多一次（D121）、P1-3 guardian 错误边界 + P1-7 budget
  衰减（D122）、P1-5 supervise 陈旧路径派生（D119）。
- 已确认待修（后续轮）：P1-1 陈旧偷锁破坏 merge 串行化（atomic.ts staleMs + merge.ts）、P1-4 chunks.yaml
  并发丢失更新（addChunkAndTask 加锁）。

## 决策编号 D119-D124（本 run 落档，详条见 docs/DECISIONS.md，catalog §26）

| 编号 | 主题 | 落点 |
|---|---|---|
| D119 | 零产出看门狗 M1（分级干预/TOOL_ENV_BROKEN 立即 at_risk/通知投递语义）| W2 watchdog |
| D120 | 工具探测 M2 + 工作房门闩 M4（含 worktreePath canonical 裁决）| W1 env-gate |
| D121 | 命令队列消费语义（P1-2 至多一次 + P1-4 截断入锁）| W2 watchdog |
| D122 | guardian 错误边界（P1-3）+ budget 衰减（P1-7）| W2 watchdog |
| D123 | 工具链脚本集与测试编排（C3）| W1 toolchain |
| D124 | 知识自主整理（kb-triage 四维评分）| 修复波 |

> 编号注：kb-triage 初始提交 a5de16f 与 r17-fix-wave-record.md 曾以「D119 预留」标记（提交时点在
> watermark 分配之前），正式分配后 kb-triage 落地为 **D124**；历史提交中「D119」字样为预留期引用，
> 以 DECISIONS 正式编号为准。

## 效率实测（全部真实执行，exit_code 实证 · 来源各 task evidence.yaml）

- **worktree-setup 自链**（env-gate E7）：纯工作房自链环境 build core/orchestrator 成功 + npm test exit 0
  （core 162 / bus 25 / orch 390 / pi-ext 36 / mcp 18 / dash 31+2skip，fail 0）+ 三 lint 0；toolchain 侧
  自链幂等两遍 + 迷你 --new + 3 类失败路径全过。
- **test-iso 全量**（toolchain）：mktemp HOME 隔离 + 先 tsc -b + 6 包并行 + dashboard vitest 59 并入，
  根 npm test 全量绿；merge-gate 四形态 + tour-check 三形态 + sh -n 5 脚本全过（sdet 持矩独立复核）。
- **kb-triage 首跑**：57 候选（1 跳过索引/模板）→ STORE 34 / STAGING 18 / IGNORE 4（自动发现 2 份字节
  重复 evolve 纪要、codename-ledger 低信号）。
- 最终态回归（watchdog evidence run-lead 复核节）：orchestrator **405/405** + 全量 6 包 fail 0 + 三 lint 0
  （persona 20 / skill 2 / decision 127）。

## 自主知识整理机制（D124）

- scripts/kb-triage.mjs：四维评分（复用性/新颖性/信号强度/行动关联 0-2）+ 一票规则（引用保护/字节重复/
  永久保留类/流水账上限）→ STORE/STAGING/IGNORE；--dry-run 默认、--apply 出报告、**永不删除文件**。
- 让 picode 自主决策存什么/忽略什么：STORE 自主执行，STAGING/删除候选批量上报 run-lead 一次审批多条。
- 与 DOC-LIFECYCLE（生命周期章程）+ docs:lean（结构检查）构成文档维护三件套；首跑报告：
  `docs/knowledge/feedback/kb-triage-run-2026-08-16T09-30-00-EFFICIENCY.md`。

## 验收结果

- decision-lint **0 error**（docs/** 全量扫描；D119-D124 落档后 REF_UNRESOLVED 清零）
- `npm run docs:lean` 通过（结构/行式/索引/冗余四项 OK；feedback/README 转正项 warn 不阻断，W3 遗留）
- 全量 npm test 绿（各 merge 门禁内）+ 三 lint 0；diff 门禁 = 写集文件 ⊆ write_paths（各 merge 已核）
- 串行 merge + push 完成；rev-list origin/main..main = 0

## 教训沉淀

- 全量调整记录 = `docs/knowledge/feedback/r17-fix-wave-record.md`（环境层/机制层/流程纪律/新机制/待修，
  后续 run 规划/招聘/巡检**必须引用**）
- 流程快赢规范 = `docs/knowledge/feedback/process-quickwin-r17.md`（增量进度格式/接管三查/at_risk 阶梯/
  评分 checklist/巡检三查/开工自检）
- 关键教训（与 D119-D124 联动）：① 工具/环境故障必须结构化上报而非静默卡死（M2）；② 工作房真实存在
  是 spawn 前置（M4 + worktree-setup 自链）；③ 机械层兜底 + 人工阶梯双轨（M1 看门狗 + process-quickwin §3）；
  ④ 代实现不豁免门禁（watchdog E5 四轮审查实证）；⑤ 测试编排根除三类假红（pnpm abort/stale dist/HOME 污染）。

## 剩余风险 / 延续项（E20 终态）

- M3 复活重投喂扩展（probeServeHealth 复活路径测试）列为 P2 延续（E5 r1/r2 记录）
- watchdog P2 延续项：bus payload meta 结构化、转录文件名子串匹配（P2-E）、supervise 接线 watchdog 留待
  交接声明、注入抛错测试
- P1-1 陈旧偷锁（atomic.ts staleMs + merge.ts）、P1-4 chunks.yaml 并发丢失更新 → 后续轮
- feedback/README.md 索引转正（DOC-LIFECYCLE §2，W3 chunk-docs 已知遗留，warn 级）
- serve 侧会话累积 GC（D109 遗留，跨轮候选延续）；流程简化 A 级试点排下一轮流程优化 run（D112/E18 延续）
- commit: 本提交（chunk-docs / W3 · run-2026-08-16T09-30-00-EFFICIENCY）
