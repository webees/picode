<!-- 文档小组产物。authored_by: docs-lead@run-2026-08-15T01-12-43-3NZ · drafted_by: tech-writer · checked_by: docs-qa · date: 2026-08-15 -->

# 交接摘要归档 — C3 死导出清理 + C2 E7/E2 排除语义修复（run-2026-08-15T01-12-43-3NZ）

> 来源：`.picode/runs/run-2026-08-15T01-12-43-3NZ/tasks/task-chunk-{dead-export-cleanup,evolve-glob-fix}/handoff/`
> （summary.md / known_issues.md / evidence.yaml / artifact_index.md 提炼；C3 交接包经 sdet 复核增补定稿）。
> 状态：两 chunk 均已由 run-lead 审查签收并合并推送——C3 = `f4c4a4b`（内容 ef25cd2）、
> C2 = `362718a`（内容 492e2ac）；main 远端已同步。本归档为正式移交留档，供 C4/C5/C6 与后续 run 复用。

---

## C3 — task-chunk-dead-export-cleanup（团队 松风：山鬼 / 湘君 / 河伯）

### 一句话

删除两个死导出 `isPicodeError`（core errors.ts）与 `canConsumeModel`（core session.ts），同步 5 处
测试引用（等价语义改写/删断言），全仓源码零残留，build 全绿、官方测试 0 新增失败，保留导出行为不变。

### 交付物

- 分支 `picode/run-2026-08-15T01-12-43-3NZ/task-chunk-dead-export-cleanup`，基线 cda6e13，
  HEAD `ef25cd2`（内容与 sdet 验证的 4834bd1 tree 完全一致，tree hash cb21c047…，evidence 对 HEAD 有效）。
- 写集 5 文件全命中：`packages/core/src/{errors.ts, errors.test.ts, session.ts, session.test.ts}` +
  `packages/orchestrator/src/session.test.ts`（diff 门禁 5/5 pass）。
- 待删符号 → 引用点清单 + 逐项核对（progress/progress.md）；交接包 summary / artifact_index /
  known_issues / diff_scope + evidence.yaml（sdet 河伯，verified_at 2026-08-15T09:05+07:00）。
- 测试引用同步明细：errors.test.ts:8,25；core session.test.ts:6,68-70；orchestrator
  session.test.ts:8,61-65（等价语义）；顺带注释级清理 session.test.ts:128 `allow_orch_force_wake`。

### 验证（sdet evidence.yaml，HOME 隔离）

- `npm test`（官方链）在 orchestrator 失败处短路，已逐包补齐：core 117/117、bus 19/19、
  orchestrator 303/304、pi-extension 17/17、dashboard-server 16/16、mcp-server 17/18。
- **0 新增失败**：base cda6e13 同环境同用例同失败对照（guardianTick / session_wake_direct 两处均为
  既有基线失败，见「已知问题」）。
- 反向 grep 三面零残留：packages src+test（exit=1 无匹配）/ dist（clean rebuild 后）/ 全仓 *.d.ts。
- `npm run build` 全绿（clean：删 dist + tsbuildinfo 后重建，杜绝增量陈旧）。
- 活跃符号守护：evolveRisk / opencodeSessionIdOf / errorCodeOf / formatPicodeError /
  HUMAN_ONLY_ROLES / SESSION_EVENTS / assertTransition / canTransition / PicodeError / ErrorCode
  全部可 import；被删符号已不在导出面（均为 false）。

### 已知问题（移交时如实上报）

1. **guardianTick 基线失败**（orchestrator self-drive.test.ts:279-300）：Bug A（deepMerge DEFAULTS
   污染）所致，修复在 C1 写集；本 chunk 不越写集，交 run-lead 按 E4 gate 裁决（C1 合并后复核转绿）。
2. **session_wake_direct 基线失败（flaky）**（mcp-server management.test.ts:110，`session_register`
   未返回 ok:true）：base 同用例同失败；本 chunk 不触碰 mcp-server；根因待 run-lead 分诊。
3. 提示性：`probeServeHealth`（:233）mock serve 启动竞态（flaky，base 同样存在）；
   `scripts/mcp/self-evolve.mjs:244` 与 docs 历史文本对已删符号的字符串提及——非 import/类型/运行时
   引用，不改（改历史日志有失真风险），由 run-lead 裁决。
4. 官方 `npm test` 的 `&&` 链短路：首个失败包处停止，全绿判定需逐包跑或先修基线失败（已逐包补齐）。

### 边界

- 写集仅 core errors/session + orchestrator session.test.ts；**未触碰** t-regression.test.ts
  （roomDisplay 归 C1）、config.ts、状态机语义（canTransition/assertTransition 未改）。
- NON_SESSION_ROLES 已不在仓内，无残留无需处理。
- C5 衔接：orchestrator session.test.ts 改写后保留 T21 集成测试语义（内联 state 断言），仍用本地
  `tmpGitRepo()`/`freshStore()`，C5 收敛时按 C5 写集处理，无冲突预期。
- 保留导出行为不变（errorCodeOf/formatPicodeError 测试仍绿）；dist 产物 gitignore 自动重建不入 diff。

---

## C2 — task-chunk-evolve-glob-fix（团队 星汉：玄冥 / 句芒 / 蓐收）

### 一句话

修复 **Bug B**：E2/E7 排除 glob 在多层并集下误拒——`layers=[knowledge,docs]` 时 docs 层 carve-out
`!docs/knowledge/**` 在扁平并集后否决 knowledge 层自己的 include `docs/knowledge/**`；
**双处同病（E2 `assertEvolveWritePathAllowed` + E7 `checkPersonas`）一并修复，口径统一为按层分组判定。**

### 交付物

- 分支 `picode/run-2026-08-15T01-12-43-3NZ/task-chunk-evolve-glob-fix`，基线 cda6e13，HEAD
  `492e2ac`（工作树 clean，diff ⊆ write_paths 5 文件）。
- `packages/core/src/evolve.ts`：新增共享判定 `isEvolveWritePathAllowed(config, evolve, writePath)`
  ——按层分组（路径 ∈ 某层 includes ∧ ∉ 该层 excludes → 放行）+ goal `forbidden_paths` 全局否决，
  **单一事实源**；`assertEvolveWritePathAllowed`（E2）改委托该判定，保留两种错误消息
  （`excluded by evolve layer` / `not inside any evolve layer`），既有 `/excluded/`、`/E2/` 断言不受影响；
  `evolveWritePaths`（扁平并集 + `!` 归一化）/`splitEvolveGlobs`/`evolveLayerGlobs`/`effectiveLayers`
  **形态未改**（mcp-server/commands 显示侧消费方不受影响，仅更新过时语义注释）。
- `packages/orchestrator/src/staffing.ts`：`checkPersonas` E7 段删除本地 `splitEvolveGlobs` +
  `excluded || !includes.some` 同病逻辑，改调 `isEvolveWritePathAllowed`（import 同步清理，
  noUnusedLocals 门禁过）；E7 消息格式不变（`E7: write_paths outside evolve layers: ...`）。
- 回归测试 +5 用例：core evolve.test.ts ×4（E2 双层放行 / 单层 docs carve-out 仍拒 / forbidden
  全局否决 + 非层内仍拒 / evolveWritePaths 并集形态契约）；orchestrator evolve.test.ts ×1（E2 多层
  放行 + docs 单层对照，**:66-81 单层断言全量保留未动）；staffing.test.ts ×2（E7 双层 knowledge 放行
  零问题 / 单层 docs 仍 3 席全报 E7）。
- 先红后绿留痕：实现前新用例以 `E2: ... excluded by evolve layer (!docs/knowledge/**)`（core/
  orchestrator）与 `E7: write_paths outside evolve layers: docs/knowledge/**`（staffing 3 席）复现失败，
  修复后全绿。

### 验证（sdet 蓐收 evidence.yaml）

- `npm run build` 0 error；针对性回归 37/37（exit 0，logs/01-targeted-regression.log）。
- 官方 `npm test`（HOME 隔离）**506/507**：唯一失败 = guardianTick 顺序污染 flake——stash 本 chunk
  改动后基线 303/304 同测试失败（logs/04），单独跑通过 1/1（logs/02）；`self-drive.test.ts` 不在本
  chunk 写集，会话状态机未触碰；orchestrator 306/307 vs 基线 303/304（+3 全过）→ **本 chunk 零新增失败**。
- diff 门禁：`git diff --name-only` = 写集 5 文件，⊆ write_paths（diff_scope.md）。

### 已知问题（KI）

- **KI-1 guardianTick 基线 flake**：归因方向 = C1（deepMerge DEFAULTS 污染），C1 acceptance 含
  「guardianTick 顺序依赖用例全绿」；本 chunk 不越写集代修（`self-drive.test.ts` 不在写集）。
- **KI-2 工作房 node_modules 断链**：`node_modules/@picode/{core,bus}` 原指向不存在的
  `.picode/node_modules` → 跨包解析落主仓陈旧 dist；已重建为本工作房 packages 自链（gitignored
  零 repo diff），sdet/审查复建环境须重复此步骤。
- **KI-3 C5 迁移范围**：`staffing.test.ts` 新增 `selfEvolveRun` 助手 + `writeEvolvePersonas`、
  `evolve.test.ts` 既有 `tmpGitRepo` 使用点须纳入 C5（test-fixtures-unify）迁移；新断言结构纯内存
  tmp 仓库、可平滑迁移，迁移后行为不变。
- **KI-4 语义契约（勿回退）**：E2/E7 判定已统一到 core `isEvolveWritePathAllowed`——carve-out 只否决
  其所属层；`forbidden_paths` 全局否决；单层 docs 对 `docs/knowledge/**` 仍拒（orchestrator
  evolve.test.ts:66-81 硬约束）；错误消息可匹配 token（`excluded` / `E2` / `E7: write_paths outside
  evolve layers`）不得改。
- 本会话无独立 engineer/sdet 子代理席位，三席职责按岗位纪律内联执行（先红后绿 + evidence 取证），
  证据日志齐全可独立审查复跑。

### 边界

- 写集 5 文件：core {evolve.ts, evolve.test.ts} + orchestrator {staffing.ts, staffing.test.ts,
  evolve.test.ts}；未触碰 mcp-server / self-drive.test.ts / config.ts。
- 单层语义完全不变（E7 单层 docs 仍 3 席全报）；显示侧（evolveWritePaths 并集形态）契约未动。

---

## 合并与后续衔接（对 C4/C5/C6）

- 合并顺序（D036 串行列车）：C3 = `f4c4a4b`（abe9b58..f4c4a4b）→ C2 = `362718a`（f4c4a4b..362718a），
  均经 run-lead 审查（conclusion: pass）+ evidence PASS + tpm 签收 + 三角解散；main 远端已同步
  （push 成功）。
- **E4 gate 复核点**：官方 npm test 全绿门槛被 guardianTick（C1 修复范围，已含 co-001 后全绿）与
  session_wake_direct（flaky 待分诊）共同阻断；C1 合并后须复核「guardianTick 转绿 + 全量无新增失败」。
- **C5 输入**：以上 KI-3 / C3 known_issues #4 的夹具迁移清单即为 C5 收敛范围输入。
- **C6 输入**：两 chunk 的 commit、证据索引、基线失败记录与裁决建议供 E16 纪要 / DECISIONS / catalog
  引用（本归档已提炼）。
