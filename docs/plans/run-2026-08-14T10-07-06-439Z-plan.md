# run-lead 自治规划 — 续跑摘要 stripNoise 口径统一 + supervise 监控命令正式化（run-2026-08-14T10-07-06-439Z · self_evolve · scale L）

> 目标（宽松，run-lead 自主决策）：
> 1. **续跑摘要 ready/re-spawn 收拢（D081 缓项落地）**：feed 与 re-spawn 的 `stripNoise` 口径列表统一为单一共享常量，抽离中立模块**消除循环依赖**；checkpoint 剔噪口径同源
> 2. **监控/守护正式化**：`supervise.mjs` 监控循环进产品能力——serve token 轮询上移 orchestrator + 新增 `picode supervise` 命令（复用 statusSnapshot/live tokens，STOPPED 空闲判定纯函数）；feed.mjs 的 spawn/ask/poll 映射已有产品能力（session wake / continuation_feed MCP / session audit / live tokens）留档
> 3. 其他缓/拒项留档（checkpoint 进 statusSnapshot 三面、自动捕获默认开启评估、feed.mjs 专项命令）
>
> 背景：D077（feed 剔 `[READY_MESSAGE_TEXT, CONTINUATION_PROMPT]`）与 D083（re-spawn 仅剔 `[READY_MESSAGE_TEXT]`）落地后，D081 缓项「ready/re-spawn 列表收拢」留档——根因是共享常量模块若由任一模块反引会构成循环依赖（`continuation.ts` 已导入 `opencode-adapter.ts` 的 `READY_MESSAGE_TEXT`）。本轮把常量下沉中立模块解决。监控侧 `scripts/supervise/*.mjs` 仍为硬编码 `/tmp/picode-dogfood` 的 dogfood 脚本，supervise.mjs 的监控循环（token/会话/worktree/任务轮询 + STOPPED 判定）无产品化出口；session audit 已产品化（D073），剩余 supervise/feed 未收编。
>
> 依据：`packages/orchestrator/src/{continuation.ts,opencode-adapter.ts,checkpoint-store.ts}`（stripNoise 三处消费）、`packages/dashboard-server/src/live.ts`（serve token 轮询原语，仅依赖 @picode/core，可上移）、`status.ts` statusSnapshot（纯读投影，supervise 复用）、`commands/supervise` 形态参照 `commands/session.ts`（D073 session audit 先例）。
> 基线：main = 5945c68；实测 448 tests 全绿（core 111 / bus 19 / orchestrator 268 / pi-extension 17 / mcp-server 17 / dashboard-server 16）；`npm run check` 三 lint 0 error（decision-lint 仅 1 条历史 warning，非本轮引入）。
>
> 编号约定：本 run 决策按水位预留 **D092-D094**（已 `reserve --run run-2026-08-14T10-07-06-439Z --count 3`，start 92）。

---

## (a) 决策清单

### D092 续跑摘要 stripNoise 口径列表统一（feed 与 re-spawn 收拢 · 本轮核心 1）

| # | 问题 / 候选 | 处置（决定） | 理由 |
|---|---|---|---|
| D092-1 | **口径漂移**：feed（continuation.ts:193）剔 `[READY_MESSAGE_TEXT, CONTINUATION_PROMPT]`，re-spawn（opencode-adapter.ts:321）仅剔 `[READY_MESSAGE_TEXT]`，checkpoint（checkpoint-store.ts CHECKPOINT_NOISE）剔 `[READY, CONTINUATION]`——三处列表不一致，re-spawn 摘要可能残留 CONTINUATION_PROMPT 噪音 | **新增中立模块 `packages/orchestrator/src/summary-noise.ts`**：下沉 `READY_MESSAGE_TEXT`、`CONTINUATION_PROMPT`、`CONTINUATION_SUMMARY_HEADER` 常量 + 导出 `SUMMARY_STRIP_NOISE = [READY_MESSAGE_TEXT, CONTINUATION_PROMPT]`（readonly，零依赖）；opencode-adapter.ts / continuation.ts 从该模块导入并**保留 re-export**（测试与既有导入方兼容），feed、re-spawn、checkpoint 统一消费 `SUMMARY_STRIP_NOISE` | 根因修复：列表收拢需共享常量，但 `continuation→opencode-adapter→continuation` 双向引会成环——常量下沉到零依赖模块后环不存在；单点真相杜绝再次漂移（D081 缓项闭环） |
| D092-2 | **re-spawn 行为变更**：re-spawn 现仅剔 READY，收拢后同剔 CONTINUATION_PROMPT | **有意变更**：re-spawn 转录可能含此前 continuation feed 注入的 `CONTINUATION_PROMPT`，剔除更正确（口径与 feed/checkpoint 完全一致）；既有 2 个 re-spawn 去噪单测扩断言 | D081 缓项原意即「完全一致化」；行为收敛到统一列表 |
| D092-3 | **循环依赖验证**：`continuation.ts` 已 import `opencode-adapter.ts`（READY/OpencodeSpawner） | `summary-noise.ts` 零 import；两个模块只单向依赖它。tsc 编译 + 既有模块图不受影响 | 共享常量模块的依赖方向是唯一要求：常量不得再依赖消费者模块 |

### D093 supervise 监控命令正式化（监控/守护进产品能力 · 本轮核心 2）

| # | 问题 / 候选 | 处置（决定） | 理由 |
|---|---|---|---|
| D093-1 | **serve token 轮询无产品出口**：`fetchLiveTokens` 目前只在 dashboard-server `live.ts`（仅依赖 @picode/core readYamlFile），orchestrator 无法反向依赖 dashboard-server | **上移 orchestrator**：新建 `packages/orchestrator/src/live.ts`（fetchLiveTokens/lastTokenSample/serveSessionIdOf/stripOcPrefix 原样迁移），orchestrator index re-export；dashboard-server `live.ts` 改为 `export * from "@picode/orchestrator"` 薄壳，router/index.test 改从 `@picode/orchestrator` 导入 | 依赖方向 dashboard-server → orchestrator 不变；token 轮询成为平台原语，supervise 与 dashboard 共用一份（D070「薄 HTTP 包装避免第二份解析逻辑」延续） |
| D093-2 | **supervise.mjs 监控循环无产品化**：硬编码 `/tmp/picode-dogfood`，轮询逻辑与 statusSnapshot/live.ts 重复 | **新增 `picode supervise` 命令**：`--run <id>` + `--once`（默认，单次观测输出 JSON）/ `--interval <sec>`（循环，含 STOPPED 判定）。观测纯函数 `deriveSuperviseObservation(dir, config, {now?, fetchImpl?})`（复用 statusSnapshot + 每 awake 会话 fetchLiveTokens + worktree 文件数），STOPPED 判定 `isIdleStopped` 纯函数（全体 token 连续 3 轮零增长）；无平台 daemon（操作者前台调用，类比 dashboard-server 常驻服务） | 监控/守护成为产品能力，复用既有纯读投影（D002 文件真相、D037 无 daemon 不变量延续）；命令表进 --help（D074 模式） |
| D093-3 | **feed.mjs spawn/ask/poll 正式化** | **缓（映射已有能力，不新建命令）**：spawn ≈ `session wake`、ask ≈ `continuation_feed` MCP、poll ≈ `supervise --once`/live tokens、残留审计 ≈ `session audit`。operations.md 写明映射，feed.mjs 保留为操作者 dogfood 脚本 | 已有能力覆盖，再造命令属重复（E11「避免第二份解析逻辑」同理）；留档 D094 |
| D093-4 | **STOPPED/空闲判定语义** | 沿用 supervise.mjs 现语义：`total` 为全体 agent token 汇总，连续 3 轮零增长 → STOPPED；POLL_FAIL（-1）不参与判定（轮询失败非空闲信号） | 与既有 dogfood 观测口径一致，纯函数可测 |

### 处置：缓 / 拒（本轮不做，留档 D094）

| # | 候选 | 处置 | 理由 |
|---|---|---|---|
| D094-1 | **checkpoint 进 statusSnapshot 三面**（status/CLI/MCP 同源展示最新 checkpoint） | **缓** | 需动 status 契约 + mcp-server（E13 后续候选 #1）；先经自动捕获跑一版观测验证价值 |
| D094-2 | **自动捕获默认开启评估**（翻转 `self_evolve.checkpoints.enabled` 默认） | **缓** | E13 后续候选 #2：观测价值验证后再评估翻转，现保守默认 false |
| D094-3 | **feed.mjs spawn/ask/poll 专项命令** | **缓** | D093-3：已有能力全覆盖，映射文档化即可 |
| D094-4 | **docs/ 历史引用债清理**（decision-lint 历史 warning） | **缓** | 非本轮引入、warning 级（E12/E13 已记录）；本轮不动旧 plan 文档 |

---

## (b) chunk 分块建议（3 个；C1/C2 代码并行实现、串行 merge 列车 D036，C3 文档收尾）

### C1 `chunk-summary-noise-unify`（stripNoise 口径统一 · 代码+测试）

- **write_paths**：
  - `packages/orchestrator/src/summary-noise.ts`（**新建**：下沉 `READY_MESSAGE_TEXT`/`CONTINUATION_PROMPT`/`CONTINUATION_SUMMARY_HEADER` 常量 + `SUMMARY_STRIP_NOISE` readonly 列表；零 import）
  - `packages/orchestrator/src/opencode-adapter.ts`（删除本地 READY 常量定义，改 `import { READY_MESSAGE_TEXT } from "./summary-noise.js"` + `export { READY_MESSAGE_TEXT }` 兼容；re-spawn `wakeWithOpencode` stripNoise 改用 `SUMMARY_STRIP_NOISE`）
  - `packages/orchestrator/src/continuation.ts`（删除本地 CONTINUATION_* 定义，改从 summary-noise 导入 + re-export；feed stripNoise 改用 `SUMMARY_STRIP_NOISE`）
  - `packages/orchestrator/src/checkpoint-store.ts`（`CHECKPOINT_NOISE = SUMMARY_STRIP_NOISE`，删除本地重复定义）
  - `packages/orchestrator/src/summary-noise.test.ts`（**新建**：常量导出 + 列表与 feed/checkpoint 一致）
  - `packages/orchestrator/src/opencode-adapter.test.ts`（扩 D083 用例：re-spawn 摘要同剔 READY 与 CONTINUATION_PROMPT）
  - `packages/orchestrator/src/checkpoint-store.test.ts`（断言 `CHECKPOINT_NOISE` 与 `SUMMARY_STRIP_NOISE` 同引用）
- **read_paths**：`transcript-store.ts`（historySummary stripNoise 契约）、现有 continuation/opencode-adapter/checkpoint-store 结构、D083 边界
- **public_contract**：`SUMMARY_STRIP_NOISE` 为唯一剔噪列表；feed/re-spawn/checkpoint 三处同源；既有 re-export 符号（READY_MESSAGE_TEXT 等）保持向后兼容
- **depends_on**：无（与 C2 不同文件）
- **验收口径**：
  - C1-a `command`：`npm run build && npm test` 全绿（既有 448 无回归）
  - C1-b 单测：`summary-noise.test` 断言列表含两者且无重复；re-spawn 单测断言摘要不含 CONTINUATION_PROMPT；checkpoint 单测断言 CHECKPOINT_NOISE === SUMMARY_STRIP_NOISE
  - C1-c 核查：无循环依赖——`tsc -b` 干净 + 模块图 review（summary-noise 零 import）
  - C1-d `command`：`npm run check` 三 lint 0 error
- **P07 门禁**：diff 仅 `packages/orchestrator/src/{summary-noise.ts,opencode-adapter.ts,continuation.ts,checkpoint-store.ts}` + 对应 test

### C2 `chunk-supervise-command`（supervise 监控命令正式化 · 代码+测试）

- **write_paths**：
  - `packages/orchestrator/src/live.ts`（**新建**：自 dashboard-server live.ts 原样迁移 fetchLiveTokens/lastTokenSample/serveSessionIdOf/stripOcPrefix + 类型；仅依赖 @picode/core）
  - `packages/orchestrator/src/supervise.ts`（**新建**：`deriveSuperviseObservation(dir, config, {now?, fetchImpl?})`——statusSnapshot + 每 awake 会话 fetchLiveTokens（fetchImpl 可注入 mock）+ worktree `.ts` 文件数，输出 `{ts, agents[{agent_id,state,tokens}], total, worktrees, tasks, merge_queue}`；`isIdleStopped(observations, {rounds, thresholdRounds})` 纯函数——total 连续 3 轮零增长）
  - `packages/orchestrator/src/commands/supervise.ts`（**新建**：`picode supervise --repo <path> --run <id> [--once|--interval <sec>] [--log <path>]`；`--once` 单次 JSON；`--interval` 循环 + STOPPED 判定 + `--log` 追加 JSONL；POLL_FAIL 不参与判定）
  - `packages/orchestrator/src/commands/index.ts`（注册 superviseCommands + DOMAIN_ORDER 加 `"supervise"`）
  - `packages/orchestrator/src/index.ts`（`export * from "./live.js"` + `export * from "./supervise.js"`）
  - `packages/orchestrator/src/live.test.ts`（**新建**：迁移 dashboard-server live 4 用例 + orchestrator 导出断言）
  - `packages/orchestrator/src/supervise.test.ts`（**新建**：derive 观测形状/awake 会话 token/POLL_FAIL 不判 idle/STOPPED 纯函数判定；fetchImpl mock）
  - `packages/orchestrator/src/cli.test.ts`（--help 命令表加 `picode supervise --repo` + domain `supervise:`）
  - `packages/dashboard-server/src/live.ts`（改为 `export * from "@picode/orchestrator"` 薄壳）
  - `packages/dashboard-server/src/router.ts` + `src/index.test.ts`（`./live.js` → `@picode/orchestrator`）
- **read_paths**：`status.ts`（statusSnapshot/ContinuationTelemetry）、`session-store.ts`、`commands/session.ts`（D073 audit 命令形态）、dashboard-server live.ts/router.ts 现实现
- **public_contract**：`picode supervise` 命令；`fetchLiveTokens` 等经 @picode/orchestrator 导出（dashboard-server 兼容）
- **depends_on**：无（与 C1 不同文件）
- **验收口径**：
  - C2-a `command`：`npm run build && npm test` 全绿（既有 448 无回归）
  - C2-b 单测：deriveSuperviseObservation 形状（agents/total/worktrees/tasks/merge_queue）；`fetchLiveTokens` 迁移用例 4/4 通过；`isIdleStopped` 连续 3 轮零增长 → true、中间有增长 → false、POLL_FAIL 不计入
  - C2-c 核查：`cli.test` --help 含 `picode supervise` + `supervise:` 组；dashboard-server 测试全绿（live 从 orchestrator 再导出）
  - C2-d `command`：`npm run check` 三 lint 0 error
- **P07 门禁**：diff 仅 `packages/orchestrator/src/{live.ts,supervise.ts,commands/supervise.ts,commands/index.ts,index.ts,live.test.ts,supervise.test.ts,cli.test.ts}` + `packages/dashboard-server/src/{live.ts,router.ts,index.test.ts}`

### C3 `chunk-supervise-docs`（DECISIONS + catalog + operations + 知识归档 · docs 层）

- **write_paths**：
  - `docs/DECISIONS.md`（D092 stripNoise 口径统一 + D093 supervise 监控命令正式化 + D094 缓/拒行；D083 详条更新「re-spawn 剔噪已与 feed 收拢为统一列表」）
  - `docs/reference/decision-catalog.md`（§12.8 摘要窗口与去噪：`SUMMARY_STRIP_NOISE` 唯一列表 + re-spawn 口径收拢；新增 supervise 监控命令节——`picode supervise --once/--interval` + STOPPED 语义 + live 原语上移）
  - `docs/guides/operations.md`（supervise 命令用法：`--once` 单次 / `--interval` 循环 / `--log` JSONL / STOPPED 判定；feed.mjs 能力映射表：spawn→session wake、ask→continuation_feed MCP、poll→supervise/live tokens、审计→session audit）
  - `docs/knowledge/evolve/run-2026-08-14T10-07-06-439Z.md`（E14 纪要：D092/D093 diff/验证/剩余风险/后续候选 + 循环依赖解法记录）
  - `docs/plans/run-2026-08-14T10-07-06-439Z-plan.md`（本规划，已含）
- **read_paths**：C1/C2 产出、DECISIONS 现状、catalog §12.8/§12.9、operations 现有 session/checkpoint 节、D083 详条、E13 纪要
- **depends_on**：C1、C2（机制落地才写得准；`npm run check` 在 C2+C3 齐后全绿）
- **验收口径**：
  - C3-a `command`：`npm run build && npm test` 全绿 + `npm run check` 三 lint 0 error
  - C3-b 核查：DECISIONS 含 D092/D093（表行+详条）与 D094（缓/拒行）；D083 详条已更新收拢说明；无重复编号、表↔详条对齐
  - C3-c `command`：`node docs/decisions/reserve.mjs --land --run run-2026-08-14T10-07-06-439Z` + `node packages/core/dist/validate/decision-lint.js .` 0 error
  - C3-d 核查：catalog §12.8 + supervise 节 + operations 映射表齐全；E14 纪要含循环依赖解法与 STOPPED 语义
  - C3-e 核查：D094 缓/拒行留档（checkpoint 三面 / 自动捕获默认评估 / feed.mjs 专项命令 / docs 引用债）

**编排**：C1（stripNoise 统一）与 C2（supervise 命令）文件无重叠，可并行实现、串行 merge（D036）；C3（docs）在 C1/C2 合并后收尾。E4 gate：代码层显式 `npm run build && npm test`；merge_ready 强制唤醒 code-review（E5，code 层 MUST）；C3 land 后 reservations 标记 landed。

---

## (c) 实施者分配

| 任务 | 实施方 | 说明 |
|---|---|---|
| 决策清单（本文档 D092–D094） | run-lead（本会话） | 已产出；编号已 reserve（D092-D094） |
| C1 summary-noise-unify | **三角 A**（squad-lead/engineer/sdet，真招聘） | 常量下沉中立模块 + 三处 stripNoise 收拢 + 测试；sdet 验证循环依赖消除 + re-spawn 新断言 |
| C2 supervise-command | **三角 B**（squad-lead/engineer/sdet，真招聘） | live 原语上移 + `picode supervise` 命令 + 观测/STOPPED 纯函数；sdet 验证形状/判定/命令表 |
| C3 supervise-docs | **文档小组**（docs-lead/tech-writer/docs-qa） | DECISIONS D092-D094 + catalog + operations 映射 + E14 纪要 + `--land` 收尾 |
| 评审 | code-review（E5 code 层 MUST） | C1/C2 merge_ready 机械唤醒 |

人员调度：C1/C2 各一三角经标准 staffing 真招聘（D025/D030），可并行开工；C3 文档小组在 C1/C2 合并后收尾。改动集中在 `packages/orchestrator/src/{summary-noise,live,supervise,opencode-adapter,continuation,checkpoint-store}.ts` + commands 注册 + `packages/dashboard-server/src/{live,router}.ts` + docs 层，**不触碰** statusSnapshot 契约顶层 / mcp-server / bus / checkpoint-store 原语 / 规则表。

---

## (d) 后续候选（本轮不做，留档）

1. **checkpoint 进 statusSnapshot 三面（D094-1）**：status/CLI/MCP 同源展示最新 checkpoint；需动 status 契约 + mcp-server，先经自动捕获验证观测价值
2. **自动捕获默认开启评估（D094-2）**：观测价值验证后翻转 `self_evolve.checkpoints.enabled` 默认（现保守 false）
3. **feed.mjs spawn/ask/poll 专项命令（D094-3）**：已映射 `session wake` / `continuation_feed` MCP / `supervise` / `session audit`，不新建
4. **docs/ 历史引用债清理（D094-4）**：decision-lint 历史 warning（旧 plan 的悬空编号引用）人工清零（E12/E13 已记录）
5. **maxTokens 真计量**：待 serve token 契约（D058）就绪

---

## 本轮验证载体

无人干预下由 self-drive guardian 推进（三角会话 ready → 自主实现 → 续跑 → 自测 → evidence/handoff → 串行 merge）。
验收判定：C1/C2 代码任务合并入 main（acceptance 1/2 达成——stripNoise 口径统一 + supervise 监控命令正式化），C3 文档合并（acceptance 3/4 达成——DECISIONS canonical + 既有单测全绿 + `npm run check` 三 lint 通过 + `--land` 完成），E14 纪要归档。**额外 dogfood**：supervise 命令对真实 run 仓 `--once` 观测即首次真实运行验证。
