# run-lead 自治规划 — checkpoint 观测三面同源 + 自动捕获默认开启 + merge 后自动 push 远端（run-2026-08-14T11-14-26-837Z · self_evolve · scale L）

> 目标（宽松，run-lead 自主决策 + sponsor 要求）：
> 1. **checkpoint 进 statusSnapshot 三面**（E13/E14 后续候选 1）：status/CLI/MCP 同源展示最新 checkpoint——
>    仿 D069 continuationTelemetry 模式，新增纯读 `checkpointTelemetry`，statusSnapshot 加 `checkpoint` 段
>    （`picode status` 自动同源），MCP 新增 `checkpoint_status` 工具（镜像 `continuation_status`）；为
>    D091 自动捕获提供「观测价值」验证面
> 2. **自动捕获默认开启评估**（E13/E14 后续候选 2）：`self_evolve.checkpoints.enabled` 默认翻转 false→true——
>    评估依据：D091 捕获为纯观测（D082 只写不读边界不变、guardian 600s 节流、merge 前 best-effort 不阻断）
> 3. **merge 后自动 push 远端机制**（**sponsor 要求**）：merge process 在合并成功后自动 `git push origin`
>    ——让「及时推送」成为 picode 自身流程而非依赖监督者收尾；best-effort 绝不阻断 merge
> 4. 其他评估项留档 D097（feed 映射文档化 / 摘要语义化 / docs 引用债——D094 延续，不实现）
>
> 背景：D082 checkpoint MVP 仅 CLI 消费面（`picode checkpoint status`）；D091 自动捕获（guardian 周期 +
> merge 前）落地但默认 `enabled: false` 保守关闭，且 statusSnapshot（D069 三面同源模式）无 checkpoint
> 观测段——run 全局视角下 checkpoint 状态不可见，自动捕获跑出的快照无观测出口，「观测价值」无从核验。
> E13/E14 把「checkpoint 进 status 三面」与「自动捕获默认开启评估」列为后续候选 1/2，本轮落实。
> **sponsor 转达一（及时推送，本轮新增）**：产出要及时推送远端，不要等监督者收尾；merge 后自动 push
> 机制化（D098 + C2 代码）；本轮 C3 merge 后即 push origin main（监督者已在做，纳入 C3 收尾执行步骤）。
> **sponsor 转达二（吞吐反馈，本轮新增）**：推进速度慢、每次只改一点点代码；要求提高吞吐——见下节
> 「sponsor 吞吐反馈处置」。
>
> 依据：`packages/orchestrator/src/{checkpoint-store.ts,status.ts,continuation.ts}`（checkpoint 只读原语 +
> D069 continuationTelemetry 三面同源先例）、`packages/mcp-server/src/management.ts`（`continuation_status`
> 工具镜像模板）、`packages/core/src/config.ts`（CheckpointCaptureConfig/GitConfig DEFAULTS）、
> `self-drive.ts` guardianTick / `merge.ts` mergeNext（D091 自动捕获接线点 + 本轮 push 接线点）。
> 基线：main = fb0095c（scripts-only 装配提交，无 TS 改动，clean 基线测试全绿）；主仓 root worktree 存在
> 既有未提交 staged 改动（goal/closure/audit 相关 9 个 orchestrator 用例环境性失败，非 checkpoint 区域、
> 非本轮引入）——任务实现/验收一律在基于 clean main 的 task worktree 进行。
>
> 编号约定：本 run 决策按水位预留 **D095-D098**（已 `reserve --run run-2026-08-14T11-14-26-837Z --count 4`，start 95；D098 为 sponsor 及时推送机制化追加）。

---

## sponsor 吞吐反馈处置（sponsor 转达二 · 采纳/调整/不采纳及落地）

| # | sponsor 建议 | 处置 | 落地 |
|---|---|---|---|
| 1 | **并行三角数 2 → 3**（写集互斥下可并行） | **采纳机制 + 如实说明**：`scheduler.max_parallel_triads` 配置已是 **3**（config.yaml + core DEFAULTS），并行上限不构成瓶颈；真正瓶颈是**写集单写者**——本轮 C2（checkpoints 默认）与 C4（git.auto_push）同改 `config.ts` 强制串行 | 本轮按写集互斥调度：C1 ∥ C2（合并后两代码 chunk 写集互斥）双三角并行；**调度规则写入 C3 docs**（PROCESSES/operations）：写集两两互斥即可并行，单写者文件（如 config.ts）内合并为同一 chunk 或串行——并行度以写集为准，不超 max_parallel_triads=3 |
| 2 | **会话回合预算 turns 1 → 2**（单次 wake 连续实施更多） | **不采纳（现状无该旋钮）**：代码库不存在 `turns: 1` 默认——`self_evolve.budgets.maxTurns` 已是 **20**（防失控远界，DEFAULT 未改），`session.budget.turns` 是**累计计数器**（session-store.ts:131 每次醒→睡 +1），非每次 wake 限额；本轮**无对应配置可翻** | 事实记录入 E15 纪要；若 sponsor 观察到的「每次只改一点点」源于 agent 单次 wake 产出粒度，杠杆在 opencode 会话侧而非 picode 预算；本轮用分块合并（建议 3）+ 并行（建议 1）提高单任务吞吐 |
| 3 | **分块粒度放宽**（相邻小项合并成一个 chunk） | **采纳**：C2（checkpoints 默认翻转）与 C4（merge 自动 push）同改 `config.ts`，合并为单一代码 chunk「C2 checkpoint-and-push」，消除同文件串行 | merge 门从 3 降 2：C1 ∥ C2 并行 → C3 收尾；单 chunk 内一次改完 config.ts 两个默认（checkpoints.enabled + git.auto_push），减少一次完整 merge/verify 周期 |
| 4 | **其他吞吐改进**（批量 merge、验证并行等） | **部分评估**：①批量 merge（一次合多个 ready chunk）**缓**——E4 gate 要求每次 merge 前 verify_commands 全量门禁 + D036 串行 merge 不变量，批量合并会弱化逐 merge 门禁，且锁持时间变长，收益不抵风险；②验证并行 **无动作**——`npm run build && npm test` 已是全仓级，node --test 默认文件级并行；③C3 docs 提前起草 catalog/operations 中与代码解耦的章节（合并后按代码实际补准）可压缩收尾等待 | 批量 merge 记录入「后续候选」；本轮不引入新旋钮 |

**吞吐约束不变**：E4 gate 全量测试、写集互斥、决策编号水位（D089 机制）——三条均为 MUST。

---

## (a) 决策清单

### D095 checkpoint 进 statusSnapshot 三面（status/CLI/MCP 同源展示最新 checkpoint · 本轮核心 1）

| # | 问题 / 候选 | 处置（决定） | 理由 |
|---|---|---|---|
| D095-1 | **checkpoint 无 run 全局观测面**：D082 仅有 `picode checkpoint status`（per-task 详单）；`picode status` / MCP `run_status` 均不展示 checkpoint 状态——D091 自动捕获产出无法从 run 视角核验 | **新增纯读派生 `checkpointTelemetry(dir, config)`**（`packages/orchestrator/src/checkpoint-telemetry.ts`，仿 `continuationTelemetry`）：复用 checkpoint-store 只读原语（`listCheckpointTasks`/`listTaskCheckpoints`），输出 `{ total, tasks_with_checkpoints, boundary_counts{manual,guardian,pre_merge}, tasks:[{task_id,count,latest_at,latest_boundary,task_status}], config:{enabled,guardian_interval_sec,pre_merge} }` | 与 D069 续跑遥测同构：纯读投影、零写，三面共享单一派生保证口径一致；`config` 段顺带暴露自动捕获开关现状，支撑 D096 评估 |
| D095-2 | **status 契约不动则 CLI 不展示** | **statusSnapshot 增 `checkpoint` 段**（`StatusSnapshot` 接口 additive 加字段，非破坏）并接线 `checkpointTelemetry`；`picode status` 命令零改动自动同源展示 | 三面之首（status）同源；additive 字段不破坏既有消费方（D069 续跑段同款先例） |
| D095-3 | **MCP 面缺 checkpoint 工具**：`run_status` 经 statusSnapshot 自动含段，但缺专用查询工具 | **新增 MCP `checkpoint_status`**（`management.ts`，镜像 `continuation_status`，纯读零写）：返回 `checkpointTelemetry(dir, config)` 全量 + 每 task 最新 checkpoint 摘要 | MCP 面齐平 continuation 三面（`run_status` 含段 + 专用工具）；纯读路径零写（D082 边界延续） |
| D095-4 | **`picode checkpoint status` 与 telemetry 口径漂移风险** | 两者复用同一只读原语（listCheckpointTasks/listTaskCheckpoints），不另造派生；checkpoint-telemetry 只做聚合 | 单点真相（D092 剔噪口径统一同理）：聚合层不同不重算底层 |
| D095-5 | **快照只读边界（D082）** | `checkpointTelemetry` 严格只读（fs 读 + 内存聚合），无任何写/锁/捕获触发；statusSnapshot 保持纯读投影 | D082/D091 不变量延续：观测面不得反驱动任何捕获/状态决策 |

### D096 自动捕获默认开启（`checkpoints.enabled` 默认翻转 false→true · 本轮核心 2）

| # | 问题 / 候选 | 处置（决定） | 理由 |
|---|---|---|---|
| D096-1 | **自动捕获默认关闭**：D091 保守 `enabled: false`，需显式开启才有 guardian 周期 + merge 前捕获 | **评估通过，翻转默认 true**：`packages/core/src/config.ts` DEFAULTS `checkpoints.enabled: false→true`；`guardian_interval_sec: 600` 与 `pre_merge: true` 默认不变 | 评估依据：①D091 捕获为**纯观测**——只写观测文件、不读 checkpoint 驱动任何恢复/续跑/调度/合并状态决策（D082 边界）；②guardian 600s 节流 + merge 前 best-effort（try/catch 绝不阻断 merge）双闸；③D095 落地后 checkpoint 有 run 全局观测面，「观测价值」可实时核验 |
| D096-2 | **翻转对既有 run 的行为影响** | 既有 run 下次 guardian tick 起自动产出 checkpoint（boundary=guardian），merge 前捕获随之生效；纯观测、不改变任何状态机行为；显式 `enabled: false` 仍可关闭（用户配置覆盖默认） | 捕获非破坏性（不覆盖既有文件，不可变落盘，D082）；边界是文件增量而非行为变更 |
| D096-3 | **回滚路径** | 配置 `self_evolve.checkpoints.enabled: false` 即恢复显式捕获行为（D082）；config 校验（D091）与 default 单测保证回滚可测 | 评估有退出闸：观测到成本/干扰异常可立即显式关闭（docs 写明） |
| D096-4 | **边界重审计** | sdet 重验 D082 边界：默认开启后 checkpoint 仍仅写不读，guardian/merge 捕获结果不进入任何状态决策路径（self-drive/merge 测试补默认开启断言） | D091 原 sdet 审计 PASS 在 enabled=false 下；翻转后须复验（E4 门禁） |

### D098 merge 后自动 push 远端（「及时推送」机制化 · sponsor 要求 · 本轮核心 3）

| # | 问题 / 候选 | 处置（决定） | 理由 |
|---|---|---|---|
| D098-1 | **产出滞留本地**：merge 落地 main 后不 push 远端，远端反映滞后、依赖监督者手工收尾 push | **mergeNext 成功后自动 push**：新增 `git.auto_push` 配置（默认 **true**）；`mergeNext` 合并成功后（`status === "merged"` 且 `auto_push`）best-effort `git push origin <base_branch>`；`MergeOutcome` 增 `push` 观测字段 `{pushed, error} \| null` | sponsor 明确要求「及时推送成为 picode 自身流程」；merge 是唯一落点（D036 串行 merge 列车），在落点接线最单一可靠 |
| D098-2 | **push 失败不得破坏 merge 可靠性** | push 全程 try/catch：失败仅记录 `outcome.push = {pushed:false, error}`，merge 状态仍为 merged、队列照常推进；与 D091 pre_merge 捕获同款「best-effort 纯观测，绝不阻断 merge」语义 | push 是增强不是门禁；远端失联/凭据失败时 merge 正确性不受影响，失败可观测（supervise/status 可查） |
| D098-3 | **无远端/不想自动推送的场景** | `git.auto_push: false` 显式关闭；`pushToOrigin` 纯函数（`execFileSync git push` 失败 → `{pushed:false,error}`）可单测 | 配置闸延续 D091 模式（enabled 总开关 + 显式关闭）；默认开符合 sponsor 意图 |
| D098-4 | **push 规程文档化** | `docs/guides/operations.md` 增 merge 规程 push 步骤（合并成功 → push；失败 best-effort 记录）；DECISIONS D098 详条记录语义 | 「至少记录 push 步骤到 merge 规程」sponsor 要求的下限由 D098 完整覆盖（自动 + 文档双落） |
| D098-5 | **回归防护** | merge.test 补三类用例：配置 bare origin remote 成功 merge → push.pushed=true 且远端 HEAD 更新；无 origin → merge 仍成功 + push.pushed=false + error 非空；auto_push=false → push=null | 既有 merge.test 为部分字段断言（`.merged`/`.checkpoint`），新增 `push` 字段 additive 不破坏；hermetic（测试 repo 的 origin 指向测试 bare 仓，不触平台仓） |

### D097 缓项留档（本轮不做）

| # | 候选 | 处置 | 理由 |
|---|---|---|---|
| D097-1 | **feed 映射文档化**（D094-1 延续）：summary-noise 消费方（feed/re-spawn/checkpoint）剔噪口径映射图鉴 | **缓** | 纯 docs 优化，非本轮 checkpoint/push 主线；独立 docs 任务收敛更聚焦 |
| D097-2 | **摘要语义化/关键动作提取**（D080/D094-4 延续） | **缓** | 摘要含关键动作语义需重新设计，不引 LLM 的 D003 约束下收益有限 |
| D097-3 | **docs/ 历史引用债清理**（decision-lint warning 级） | **缓** | 非本轮引入、warning 级（E12-E14 已记录）；本轮只保证新条目 lint 全绿 |

---

## (b) chunk 分块建议（3 个；C1/C2 写集互斥并行实现、C3 文档收尾——D036 串行 merge 列车）

> 分块粒度说明（sponsor 吞吐建议 3 采纳）：原规划 C2（checkpoints 默认翻转）与 C4（merge 自动 push）
> 同改 `config.ts` 单写者文件，拆分会强制串行；**合并为单一代码 chunk C2**，merge 门从 3 降 2。

### C1 `chunk-checkpoint-status-triad`（checkpoint 观测三面同源 · 代码+测试）

- **write_paths**：
  - `packages/orchestrator/src/checkpoint-telemetry.ts`（**新建**：`checkpointTelemetry(dir, config)` 纯读聚合——total/tasks_with_checkpoints/boundary_counts/tasks latest/config 现状；仅复用 checkpoint-store 只读原语）
  - `packages/orchestrator/src/checkpoint-telemetry.test.ts`（**新建**：空 checkpoints 目录形状 / 捕获后聚合 / boundary_counts 分布 / config 段回显）
  - `packages/orchestrator/src/status.ts`（`StatusSnapshot` 加 `checkpoint` 段 + 接线 `checkpointTelemetry`；`statusSnapshot` 内调用）
  - `packages/orchestrator/src/status.test.ts`（`checkpoint` 段存在 + 形状断言；空目录缺省不崩）
  - `packages/orchestrator/src/index.ts`（`export * from "./checkpoint-telemetry.js"`）
  - `packages/mcp-server/src/management.ts`（新增 `checkpoint_status` 工具，镜像 `continuation_status`：返回 `checkpointTelemetry`；`run_status` 经 statusSnapshot 自动含段）
  - `packages/mcp-server/src/management.test.ts`（`checkpoint_status` 形状 + **纯读零写断言**（对齐 continuation_status 用例）+ `run_status` 含 `checkpoint` 段）
- **read_paths**：`checkpoint-store.ts`（listCheckpointTasks/listTaskCheckpoints/latestTaskCheckpoint 契约）、`status.ts` 现结构（continuationTelemetry 模式 + StatusSnapshot 形状）、`management.ts` continuation_status 实现、`commands/checkpoint.ts`（既有 CLI 不动）
- **public_contract**：`statusSnapshot.checkpoint`（additive 字段）+ MCP `checkpoint_status` 工具 + `picode status` 展示；既有 `picode checkpoint status` 不变
- **depends_on**：无（与 C2 写集互斥——C2 不触碰本 chunk 任一文件）
- **验收口径**：
  - C1-a `command`：`npm run build && npm test` 全绿（clean main 基线 orchestrator 301 用例无回归；主仓 9 个环境性失败不在 task worktree 出现）
  - C1-b 单测：checkpoint-telemetry.test 形状/聚合/边界分布/config 回显；status.test checkpoint 段；management.test `checkpoint_status` 纯读零写
  - C1-c 核查：MCP `run_status` 响应含 `checkpoint` 段；`picode status` 输出含 `checkpoint`（dogfood 一次）
  - C1-d `command`：`npm run check` 三 lint 0 error
- **P07 门禁**：diff 仅上述 write_paths 7 个文件

### C2 `chunk-checkpoint-and-push`（checkpoints 默认开启 + merge 自动 push · 代码+测试，D096+D098 合一）

- **write_paths**：
  - `packages/core/src/config.ts`（DEFAULTS：`checkpoints.enabled: false→true` + 注释「默认开启（D096），显式 false 关闭」；`GitConfig` 接口加 `auto_push: boolean` + DEFAULTS `git.auto_push: true` + 注释「D098：merge 成功后 best-effort push origin」）
  - `packages/core/src/config.test.ts`（默认 `checkpoints.enabled: true` + 显式覆盖 `false` 生效；默认 `git.auto_push: true` + 显式覆盖 `false` 生效）
  - `packages/orchestrator/src/self-drive.test.ts`（默认开启下 guardian 周期捕获路径断言——guardianTick 产出 checkpoints 观测）
  - `packages/orchestrator/src/checkpoint-store.test.ts`（`guardianCaptureDue` 节流语义默认值回归；D082 边界只写不读断言扩展）
  - `packages/orchestrator/src/merge.ts`（新增 `pushToOrigin(repoRoot, branch)` best-effort 纯函数——`git push origin <branch>` 失败返回 `{pushed:false,error}`；`MergeOutcome` 增 `push: {pushed:boolean; error:string|null} | null`；`mergeNext` 合并成功分支接线：`status==="merged" && config.git.auto_push` → 捕获结果，try/catch **绝不阻断 merge**；默认开启下 pre_merge 捕获断言）
  - `packages/orchestrator/src/merge.test.ts`（merge 前捕获默认开启用例 + 三类 push 用例：①bare origin 成功 merge → push.pushed=true 且远端 HEAD 与本地一致；②无 origin → merge 仍 merged + push.pushed=false + error 非空；③auto_push=false → push=null；另补 verify 失败 → 不 push）
- **read_paths**：`config.ts` CheckpointCaptureConfig/GitConfig DEFAULTS、`self-drive.ts` guardianTick 接线、`merge.ts` mergeNext 现结构（D036 串行 + D091 pre_merge 捕获接线点）、`merge.test.ts` 既有 hermetic git 测试基建（init/worktree/bare remote）
- **public_contract**：`self_evolve.checkpoints.enabled` 默认 **true**（显式 false 关闭）；`git.auto_push` 默认 **true**（显式 false 关闭）；`MergeOutcome.push` 观测字段；merge 成功即尝试 push（best-effort）；D082 只读边界不变
- **depends_on**：无（与 C1 写集互斥；`config.ts` 单写者在本 chunk 内一次改完，不与其他代码 chunk 共享）
- **验收口径**：
  - C2-a `command`：`npm run build && npm test` 全绿
  - C2-b 单测：config 默认 `checkpoints.enabled:true` / `git.auto_push:true` + 显式覆盖；self-drive/merge 默认开启路径捕获断言；pushToOrigin 成功/失败两态；mergeNext 成功 merge 触发 push（bare origin 远端 HEAD 更新）；无 origin 不破坏 merge；auto_push=false 不 push；verify 失败不 push
  - C2-c 核查：`MergeOutcome` 新增 `push` 字段对既有 merge.test 断言（`.merged`/`.checkpoint` 部分断言）零破坏
  - C2-d 核查（sdet）：D082 边界复验——默认开启后 checkpoint 仅写不读、捕获结果不进入任何状态决策路径；push 为 merge 后纯观测附加、不参与 merge 状态判定（审计记录入 E15 纪要）
  - C2-e `command`：`npm run check` 三 lint 0 error
- **P07 门禁**：diff 仅上述 write_paths 6 个文件

### C3 `chunk-checkpoint-triad-docs`（DECISIONS + catalog + operations/PROCESSES + 知识归档 · docs 层 · 含 sponsor 推送执行与吞吐规程）

- **write_paths**：
  - `docs/DECISIONS.md`（D095 checkpoint 三面同源 表行+详条、D096 自动捕获默认开启 表行+详条、D098 merge 自动 push 表行+详条、D097 缓/拒行；D091 详条补「enabled 默认已翻转 D096」注）
  - `docs/reference/decision-catalog.md`（checkpoint 节：三面同源 `checkpointTelemetry` + MCP `checkpoint_status` + `picode status` 含 checkpoint 段；自动捕获默认开启 + 显式关闭/回滚方法；merge 节：自动 push 语义 + `git.auto_push`）
  - `docs/guides/operations.md`（checkpoint 观测命令：`picode status`（checkpoint 段）/ `picode checkpoint status` / MCP `checkpoint_status` 用法；自动捕获默认开启说明 + `enabled: false` 关闭；merge push 规程节——合并成功自动 push、失败 best-effort 记录、`git.auto_push: false` 关闭；**吞吐调度规程**——写集互斥下并行三角、单写者文件合并/串行、并行度 ≤ max_parallel_triads=3）
  - `docs/PROCESSES.md`（P05 调度节补写集互斥并行规则：chunk write_paths 两两互斥即可并行，共享文件（单写者）须合并同 chunk 或串行——sponsor 吞吐建议 1/3 的规程化）
  - `docs/knowledge/evolve/run-2026-08-14T11-14-26-837Z.md`（E15 纪要：D095/D096/D098 diff/验证/剩余风险/后续候选 + 默认翻转评估记录 + sponsor push 机制化记录 + 吞吐反馈处置（建议 1/3 采纳、2 不采纳原因、批量 merge 缓））
  - `docs/plans/run-2026-08-14T11-14-26-837Z-plan.md`（本规划，已含）
- **read_paths**：C1/C2 产出、DECISIONS 现状、catalog checkpoint/merge 节、operations checkpoint/merge 节、PROCESSES P05 调度节、D082/D091 详条、E14 纪要
- **depends_on**：C1、C2（机制落地才写得准；`npm run check` 在 C1+C2+C3 齐后全绿）
- **验收口径**：
  - C3-a `command`：`npm run build && npm test` 全绿 + `npm run check` 三 lint 0 error
  - C3-b 核查：DECISIONS 含 D095/D096/D098（表行+详条）与 D097（缓/拒行）；D091 详条已补翻转注；无重复编号、表↔详条对齐
  - C3-c `command`：`node docs/decisions/reserve.mjs --land --run run-2026-08-14T11-14-26-837Z` + `node packages/core/dist/validate/decision-lint.js .` 0 error + **merge 完成后 `git push origin main` 推远端（sponsor 要求 #1：监督者已在做，纳入 C3 收尾执行步骤，勿等监督者收尾）**
  - C3-d 核查：catalog checkpoint 节三面 + 默认开启/关闭方法齐全 + merge 自动 push 语义；operations 观测命令 + 回滚说明 + push 规程 + 吞吐调度规程齐全；PROCESSES P05 写集互斥规则；E15 纪要含默认翻转评估 + D082 复验 + sponsor push 机制化 + 吞吐反馈处置
  - C3-e 核查：D097 缓/拒行留档（feed 映射 / 摘要语义化 / docs 引用债）
- **P07 门禁**：diff 仅 docs 层上述文件（不触 packages/）

**编排**：C1（三面同源）与 C2（checkpoints 默认 + merge push）**写集互斥，双三角并行实现**（sponsor 吞吐建议 1/3：并行度以写集为准；config.ts 单写者已并入 C2 一次改完）；C3（docs）在 C1/C2 合并后收尾，且 C3 合并完成即 `git push origin main`（sponsor 及时推送）。E4 gate：代码层显式 `npm run build && npm test`；merge_ready 强制唤醒 code-review（E5，code 层 MUST）；C3 land 后 reservations 标记 landed + push 远端。

---

## (c) 实施者分配

| 任务 | 实施方 | 说明 |
|---|---|---|
| 决策清单（本文档 D095–D098） | run-lead（本会话） | 已产出；编号已 reserve（D095-D098，count 4） |
| C1 checkpoint-status-triad | **三角 A**（squad-lead/engineer/sdet，真招聘） | `checkpointTelemetry` 纯读聚合 + statusSnapshot checkpoint 段 + MCP `checkpoint_status`；sdet 验证形状/纯读零写/run_status 含段 |
| C2 checkpoint-and-push | **三角 B**（squad-lead/engineer/sdet，真招聘） | `checkpoints.enabled` 默认翻转 + `git.auto_push` 默认 true + mergeNext 成功后 best-effort push + MergeOutcome.push 观测；sdet 验证 push 三类用例 + 既有断言零破坏 + D082 边界复验 |
| C3 checkpoint-triad-docs | **文档小组**（docs-lead/tech-writer/docs-qa） | DECISIONS D095-D098 + catalog + operations（含 push 规程/吞吐调度规程）+ PROCESSES 写集互斥规则 + E15 纪要 + `--land` 收尾 + **merge 后 push origin main** |
| 评审 | code-review（E5 code 层 MUST） | C1/C2 merge_ready 机械唤醒 |

人员调度：C1/C2 各一三角经标准 staffing 真招聘（D025/D030），**双三角并行开工**（写集互斥，sponsor 吞吐建议 1）；C3 文档小组在 C1/C2 合并后收尾（其中 catalog/operations 与代码解耦章节可提前起草，合并后补准）。改动集中在 `packages/orchestrator/src/{checkpoint-telemetry,status,merge,index}.ts` + `packages/mcp-server/src/management.ts` + `packages/core/src/config.ts` + 对应 test + docs 层，**不触碰** checkpoint-store 只读原语 / bus / rules-engine / 快照只读边界语义 / merge 正确性路径（push 为 merge 后纯观测附加）。

---

## (d) 后续候选（本轮不做，留档 D097 + 吞吐候选）

1. **feed 映射文档化（D097-1）**：summary-noise 消费方（feed/re-spawn/checkpoint）剔噪口径映射图鉴
2. **摘要语义化/关键动作提取（D097-2）**：D080 延续；D003 不引 LLM 约束下收益有限，需重新设计
3. **docs/ 历史引用债清理（D097-3）**：decision-lint 历史 warning 人工清零（E12-E14 已记录）
4. **checkpoint 恢复/回滚面**：D082 快照只读边界明确禁止「读 checkpoint 驱动状态」，不立项
5. **push 失败自动重试/告警**：D098 落地后如观测 push 失败频繁，可加轻量重试或 supervise 观测段（留候选，不提前实现）
6. **批量 merge**（吞吐候选，本轮不采纳）：一次合多个 ready chunk 会弱化 E4 逐 merge 全量门禁 + 加长 merge.lock 持锁，收益不抵风险；如吞吐仍不足再评估（D036 串行 merge 不变量不变）

---

## (e) 下一轮排期（ponytail-audit 转达 + 吞吐延续 · 候选，供下一轮 run-lead 采纳）

> 来源：sponsor 转达监督者全仓审计（ponytail-audit，按删减量排序）；基线已推进到 main = b915220
> （rebase 冲突修复，492 tests 全绿）。排期原则沿用本轮吞吐处置：写集互斥并行、单写者文件并入同 chunk、
> 决策编号走水位（下一轮自 D099 起 `--reserve`）。

| # | 审计项 | 处置 | 下一轮分块建议 | 写集 |
|---|---|---|---|---|
| 1 | **死导出符号删除**（roomDisplay / canConsumeModel / isPicodeError / NON_SESSION_ROLES，prod 引用 0，仅测试引用） | **做** | **C1 task-dead-export-cleanup**：`orchestrator/src/status.ts` / `pi-adapter.ts` 删死导出 + 测试改内部引用（对应 export/import 同步清理） | orchestrator src 局部 + 对应 test |
| 2 | **24 处重复测试夹具单源**（tmpGitRepo / mkdtempSync 各测试文件重复） | **做（串行）** | **C2 task-test-fixtures-unify**：抽共享 test-utils 单源，24 处 `packages/*/src/*.test.ts` 收敛 | 面广（跨 packages 测试文件）；**放 C1/C3 合并后串行**避免与删导出/薄壳并入的 test 改动冲突 |
| 3 | **单导出薄壳文件并入**（mcp-server errors.ts / schema.ts、orchestrator jsonl.ts） | **做** | **C3 task-shell-file-merge**：并入调用方 + 同步调用处/测试 | mcp-server + orchestrator jsonl + 调用方 |
| 4 | **yagni 死配置**（D055 reserved 6 处零读取：idle_sleep / force_wake / evolution.enabled / sponsor approval / E6 path） | **做（谨慎）** | **C4 task-yagni-config-cleanup**：`core/src/config.ts` 移除或标记废弃；**须验证** config 分层 merge 不拒未知键（既有用户配置含该键仍可加载） | core/src/config.ts + config.test |
| 5 | **E7 校验语义 bug**（`!docs/knowledge/**` 误拒 knowledge 层） | **常规轨道** | 监督者已转常规（不进审计）；下一轮有空位则并入 C1 顺手修，否则由常规流程处理 | — |

**并行编排**：C1 / C3 / C4 三写集互斥 → **三三角并行**（sponsor 吞吐建议落地：`max_parallel_triads=3` + 本轮写集互斥规则）；C2（夹具单源）在 C1/C3 合并后**串行**；C5 docs（DECISIONS D099 起 + catalog + E16 纪要 + `--land` + **merge 后 push origin main**）收尾。验收口径复用本轮：E4 gate 全量 `npm run build && npm test`、code-review（E5）MUST、decision-lint 全绿。全部四项均为纯清理/重构，**不触碰** 状态机/checkpoint 只读边界/merge 正确性语义。

---

## 本轮验证载体

无人干预下由 self-drive guardian 推进（三角会话 ready → 自主实现 → 续跑 → 自测 → evidence/handoff → 串行 merge）。
验收判定：C1/C2 代码任务合并入 main（acceptance 1/2/3 达成——checkpoint 三面同源 + 自动捕获默认开启 +
merge 自动 push），C3 文档合并（acceptance 4/5/6 达成——DECISIONS canonical + 既有单测全绿 + `npm run check`
三 lint 通过 + `--land` 完成 + **merge 后 push origin main**），E15 纪要归档。**额外 dogfood**：C1 落地后对
真实 run 仓 `picode status` 输出含 `checkpoint` 段即观测面首次真实运行验证；C2 落地后首个 merge（C3 收尾）
即自动 push 的首次真实运行验证（C3-c 显式 push 双保险）。
