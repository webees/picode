run_id: run-2026-08-16T20-14-40-FIX500
title: "dashboard-server runs 端点 500 修复（缺失 goal.yaml 容错）+ 回归测试 + 端点级验收"
kind: fix
status: completed
scale: S
success_criteria:
  - "GET /api/runs/:id 与 GET /api/runs/:id/sessions 对全部 5 个 run 返回 200（含缺失 goal.yaml 的 run）"
  - "缺 goal.yaml 时 snapshot.goal.acceptance=0（与列表端点口径一致），不抛 TypeError"
  - "readGoal 默认值补 acceptance/product_acceptance 空数组（治本），statusSnapshot 调用处加 ?.length ?? 0 防御（双保险）"
  - "新增回归测试：status.test.ts（缺 goal.yaml 容错）+ dashboard-server index.test.ts（端点级 200）"
  - "全量 npm test 绿；串行 merge；重启 dashboard-server 实测 5 run × 2 端点全 200"
non_goals:
  - "不重构 statusSnapshot / run-store / readGoal 整体设计"
  - "不改其它端点（board/chunks/tasks/merge/gates/bus/approvals/change-orders 行为不变）"
  - "不迁移或改写 .picode/runs 下任何 run 目录数据"
  - "不推广全量防御性改造"
constraints:
  - "写集互斥；1 chunk = 1 工作房 = 1 三角；sdet evidence → 代码审查门 → 交接 → 签收 → 串行合并（D036）"
  - "diff ⊆ write_paths；review 记录 reviews/<task_id>.md"
  - "验收在真实 dashboard-server 上执行（端口 8788 已运行）"
product_acceptance:
  - "对缺 goal.yaml 的 run：GET /api/runs/:id 返回 200 且 snapshot.goal.acceptance=0，非 500"
  - "同一 run 的 GET /api/runs/:id/sessions 返回 200 且不抛错"
  - "全部 5 个 run 逐一请求两个端点均 200"
  - "GET /api/runs 列表端点行为不变（无回归）"
open_questions: []
user_confirmed_at: "2026-08-16T13:51:36Z"
created_at: "2026-08-16T20:14:40Z"
