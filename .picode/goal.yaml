run_id: run-2026-08-15T03-00-00-SUBAGENT
title: "continuable 子代理实现：durable 会话身份 + resume 续写 + 围栏 + settled 通知（蓝图 D107 落地）"
kind: self_evolve
status: active
scale: M
success_criteria:
  - "I2 durable 会话：sleep 保留/归档替代 DELETE（opencode-adapter stop 改造 + pi-adapter sleepAgent 接线）；wake resume 优先（sendReady 同会话续写，404 回退转录重投喂）——terminate 保持终态销毁"
  - "I3 子代理 spawn/注册 + 深度围栏 ≤N（默认 3）——spec-17 §4 增量"
  - "I4 父子写集继承只收窄（子 ⊆ 父 write_paths）——spec-04 §2 校验增量"
  - "I5 所有权围栏（子会话仅父可路由 + 子代理不可直接问人）——bus 校验增量"
  - "I1 投喂语义分级（followup/steer/inject S 变体 + wake 门闩）"
  - "I6 subagent-settled 机械层结算通知（复用既有事件词汇，来源标注纪律）"
  - "全量 npm test 全绿（含新增测试）；npm run check 三 lint 0 error；串行 merge 后 push origin main；E18 纪要归档"
non_goals:
  - "不引入事件溯源恢复路径（守 D002/D082）"
  - "jobs 注册表（P2）如本轮余量不足则排下轮"
  - "I7 cancel 保留队列（M 成本，排下轮）"
constraints:
  - "E4 gate：每次 merge 前全量 npm run build && npm test；代码审查（E5）MUST；写集互斥 ≤3 组并行；D036 串行 merge"
  - "决策编号 D109 起（上轮至 D108 落地）"
  - "工作区：/Users/x/Desktop/picode（main=911ea0c，562/562 绿基线）"
product_acceptance:
  - "sleep 后会话保留（GET /session/{id} 存在），wake 同会话续写（POST /session/{id}/message 计数）——mock serve 实测"
  - "深度围栏：第 4 层 spawn 拒绝（结构化错误）"
  - "写集收窄：子代理 write_paths ⊆ 父 write_paths（校验测试）"
  - "所有权：非父会话路由子代理被拒（bus 校验测试）"
  - "settled 通知：子代理终态产生机械层事件（复用词汇，无 LLM 自报）"
  - "全量绿 + 三 lint 0 + push"
acceptance:
  - "逐 chunk：evidence + code-review + 交接包 + 签收；run 级：全量绿 + push"
open_questions: []
assumptions:
  - "蓝图 D107 + Pi brief 为输入（已读）；基线行号按 main=911ea0c 重新核对"
user_confirmed_at: "2026-08-15T03:00:00Z"
created_at: "2026-08-15T03:00:00Z"
