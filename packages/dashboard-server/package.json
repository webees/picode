{
  "name": "@picode/dashboard-server",
  "version": "0.1.0",
  "description": "picode read-only dashboard HTTP server — node:http 零依赖投影 + opencode serve tokens 代理",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "picode-dashboard-server": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node --test dist/*.test.js"
  },
  "dependencies": {
    "@picode/core": "0.1.0",
    "@picode/orchestrator": "0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
